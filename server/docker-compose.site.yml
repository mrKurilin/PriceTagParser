services:
  site:
    build:
      context: ..
      dockerfile: server/Dockerfile.web
    ports:
      - "${SITE_HOST_PORT:-8081}:80"
    environment:
      - SITE_PORT=80
      - API_BASE_URL=${API_BASE_URL:-http://host.docker.internal:8080}
    restart: always
