services:
  site:
    build:
      context: ..
      dockerfile: server/Dockerfile.web
    ports:
      - "8080:8080"
    environment:
      - SERVER_PORT=8080
      - WEB_DIR=/app/web
    restart: always
