services:
  site:
    build:
      context: ..
      dockerfile: server/Dockerfile.web
    ports:
      - "${SITE_HOST_PORT:-8081}:8080"
    env_file:
      - .env
    environment:
      - APP_ROLE=site
      - SERVER_PORT=8080
      - WEB_DIR=/app/web
    restart: always
